#OPtimal

def largest(self,arr: List[int]) -> int:
        # code here
        n=len(arr)
        largest=arr[0]
        for i in range(1,n):
            if arr[i]>largest:
                largest=arr[i]
        return largest        

"method2"
print(arr[n-1])
